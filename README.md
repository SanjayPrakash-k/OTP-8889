# OTP-8889
The repository contain script which is for updating the created custom checkbox. Whenever there is a change in exiting address or new address is added to the Customer Record, the custom field should be checked. This should work only in Edit context.
